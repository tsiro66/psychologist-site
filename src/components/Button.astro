---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'nav';
  className?: string;
  [key: string]: any; // To allow for extra attributes like target, id, etc.
}

const { 
  href, 
  variant = 'primary', 
  className, 
  ...rest 
} = Astro.props;

const baseStyles = "px-7 text-sm font-semibold transition-colors duration-200 cursor-pointer min-h-[44px] inline-flex items-center";

const variants = {
  primary: " bg-lime-700 text-white hover:bg-lime-800 hover:text-white py-3",
  secondary: "bg-violet-600 text-white hover:bg-violet-700 hover:text-white py-3",
  nav: "nav-btn bg-lime-700 text-white hover:bg-lime-700 py-2"
};

const Element = href ? 'a' : 'button';
---

<Element 
  href={href} 
  class:list={[baseStyles, variants[variant], className]} 
  {...rest}
>
  <slot />
</Element>